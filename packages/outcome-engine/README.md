# outcome-engine

Pure, deterministic math: `aggregateSentiment`, `estimateProbability`,
`computeEdge`, `kellyFraction`, `proposeOrder`. No I/O, no implicit clock —
callers pass `nowMs` where it matters.
