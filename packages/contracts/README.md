# contracts (v2)

Zod schemas + TypeScript types for the v2 outcome-market trading agents.

Single source of truth for cross-service messages: outcome markets, sentiment
signals, probability estimates, proposals, risk decisions, fills, and the
event-envelope that wraps them on the bus.

The v1 (perp) contracts are frozen at `legacy/packages/contracts-v1/`.
