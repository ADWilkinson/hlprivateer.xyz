{
  "name": "@hl/privateer-hl-client",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "typecheck": "bunx tsc --noEmit",
    "test": "bunx vitest run --passWithNoTests",
    "lint": "bunx vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nktkas/hyperliquid": "0.31.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
