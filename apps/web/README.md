# web

Next.js web app that renders the ASCII trade floor in public/operator/agent modes.

## Runtime Notes

- `/floor` uses mobile-first defaults so core gameplay cards stay visible while noncritical sections start collapsed.
