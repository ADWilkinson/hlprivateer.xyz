# Core
NODE_ENV=development
# Set to 'production' explicitly in real deployments.LOG_LEVEL=info
TZ=UTC

# Public endpoints (used by API CORS + web build-time endpoints)
PUBLIC_BASE_URL=https://hlprivateer.xyz
API_BASE_URL=https://api.hlprivateer.xyz
WS_BASE_URL=wss://ws.hlprivateer.xyz
NEXT_PUBLIC_API_BASE_URL=https://api.hlprivateer.xyz
NEXT_PUBLIC_WS_BASE_URL=wss://ws.hlprivateer.xyz

# API
PORT=4000
API_RATE_LIMIT_MAX=120
API_RATE_LIMIT_WINDOW_MS=60000

# Auth (operator)
JWT_SECRET=
# REQUIRED in production: set a strong random value (or use JWT_SECRET_FILE).JWT_SECRET_FILE=
OPERATOR_LOGIN_SECRET=
# REQUIRED in production: used to bootstrap operator login (or use OPERATOR_LOGIN_SECRET_FILE).OPERATOR_LOGIN_SECRET_FILE=
OPERATOR_MFA_REQUIRED=true
OPERATOR_ADMIN_USERS=admin@local

# Redis (event bus + cache)
REDIS_URL=redis://127.0.0.1:6379
REDIS_STREAM_PREFIX=hlp

# Database (optional when DRY_RUN=true)
POSTGRES_PASSWORD=CHANGE_ME
DATABASE_URL=postgres://privateer:CHANGE_ME@127.0.0.1:5432/privateer
DATABASE_URL_FILE=

# Runtime loop
CYCLE_MS=5000
DRY_RUN=false

# Hyperliquid endpoints
HL_WS_URL=wss://api.hyperliquid.xyz/ws
HL_WS_URL_FILE=
HL_INFO_URL=
HL_INFO_URL_FILE=
HL_API_URL=
HL_API_URL_FILE=
HL_IS_TESTNET=false
HL_REQUEST_TIMEOUT_MS=10000

# Risk defaults
RISK_MAX_LEVERAGE=2
RISK_MAX_DRAWDOWN_PCT=5
RISK_MAX_SLIPPAGE_BPS=20
RISK_MAX_NOTIONAL_USD=10000
RISK_STALE_DATA_MS=3000
RISK_LIQUIDITY_BUFFER_PCT=1.1
RISK_NOTIONAL_PARITY_TOLERANCE=0.015
# Market-data seed only. Trade entry is agent-driven (dynamic short basket), so this does not control exposure.
BASKET_SYMBOLS=
BASKET_TARGET_NOTIONAL_USD=1000
RUNTIME_METRICS_PORT=9400
RUNTIME_FLAT_DUST_NOTIONAL_USD=50

# Live mode enable gate (runtime will refuse live unless explicitly approved)
HL_PRIVATE_KEY=
HL_PRIVATE_KEY_FILE=
ENABLE_LIVE_OMS=false
LIVE_MODE_APPROVED=false
LIVE_RECONCILE_OPEN_ORDER_MAX_AGE_MS=60000

# WS gateway
WS_PORT=4100
WS_ALLOW_INSECURE_TOKENS=false

# x402 (paid agent endpoints)
X402_ENABLED=true
X402_PROVIDER=facilitator
X402_VERIFIER_SECRET=
# REQUIRED in production: set a strong random value (or use X402_VERIFIER_SECRET_FILE).X402_VERIFIER_SECRET_FILE=
X402_FACILITATOR_URL=https://facilitator.payai.network
X402_NETWORK=eip155:8453
X402_PAYTO=CHANGE_ME
X402_PRICE_STREAM_SNAPSHOT="\\$0.001"
X402_PRICE_ANALYSIS_LATEST="\\$0.005"
X402_PRICE_ANALYSIS_HISTORY="\\$0.01"
X402_PRICE_POSITIONS="\\$0.01"
X402_PRICE_ORDERS="\\$0.01"

# Agent runner (LLM roles that publish tape + proposals)
AGENT_ID=privateer-floor
AGENT_LLM=codex
# Agent CLI auth mounts (container runtime paths)
# - For local development, mount host auth dirs so claude/codex can authorize.
AGENT_RUNNER_CLAUDE_DIR=/home/dappnode/.claude
AGENT_RUNNER_CODEX_DIR=/home/dappnode/.codex
AGENT_RUNNER_CLAUDE_CFG_DIR=/home/dappnode/.config/claude
CLAUDE_CLI_PATH=/usr/local/bin/claude
CODEX_CLI_PATH=/usr/local/bin/codex
# CLI auth tokens (optional; empty values are skipped)
OPENAI_API_KEY=
OPENAI_API_BASE_URL=
OPENAI_ORG_ID=
OPENAI_API_KEY_FILE=
ANTHROPIC_API_KEY=
ANTHROPIC_API_KEY_FILE=
CLAUDE_CODE_API_KEY=
CLAUDE_CODE_API_KEY_FILE=
#
# Scheduling knobs
# - Proposal loop is deterministic (no LLM tokens); keep it reasonably frequent for timely rebalances.
# - Analysis/risk/research loops use LLMs; increase these aggressively to save tokens for macro-style holds.
AGENT_PROPOSAL_INTERVAL_MS=30000
# Strategist directive loop (LLM-guided pivots: rotate basket / scale notional / exit).
AGENT_DIRECTIVE_INTERVAL_MS=900000
AGENT_NOTIONAL_MULTIPLIER_MIN=0.25
AGENT_NOTIONAL_MULTIPLIER_MAX=3
AGENT_MIN_REBALANCE_LEG_USD=0
AGENT_ANALYSIS_INTERVAL_MS=1800000
AGENT_RISK_INTERVAL_MS=600000
AGENT_RESEARCH_INTERVAL_MS=3600000
AGENT_OPS_INTERVAL_MS=5000
AGENT_BASKET_REFRESH_MS=43200000
CLAUDE_MODEL=opus
CODEX_MODEL=gpt-5.3-codex-spark
CODEX_REASONING_EFFORT=xhigh
AGENT_FEATURE_WINDOW_MIN=240
AGENT_FEATURE_CONCURRENCY=6

# CoinGecko Pro (optional; enriches basket selection + analysis context)
COINGECKO_API_KEY=
COINGECKO_BASE_URL=https://pro-api.coingecko.com/api/v3
COINGECKO_TIMEOUT_MS=2000

# Observability
OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4317
