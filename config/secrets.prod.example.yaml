services:
  api:
    JWT_SECRET: "<replace_with_random_api_jwt_secret>"
    X402_VERIFIER_SECRET: "<replace_with_x402_verifier_secret>"
  runtime:
    DATABASE_URL: "postgres://privateer:replace-me@127.0.0.1:5432/privateer"
    HL_WS_URL: "wss://api.hyperliquid.xyz/ws"
    LIVE_OMS_API_KEY: "<replace_with_live_oms_key>"
  cloudflare:
    CLOUDFLARED_TUNNEL_CREDENTIAL: "<replace_with_base64_cloudflared_tunnel_credential>"
