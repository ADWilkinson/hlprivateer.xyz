# HL Privateer — v2: sentiment-driven outcome market trading on Hyperliquid HIP-4
# https://hlprivateer.xyz

## What this is

An experimental, self-hosted trading agent for HIP-4 outcome contracts on
Hyperliquid. Sentiment in, probability estimate out, fail-closed risk gates,
order placement on ALLOW.

Outcome contracts are binary: settle 0 or 1 in USDH on the same CLOB as
spot/perp. Price ∈ [0,1] is implied probability.

The previous experiment (v1, perp discretionary trading desk with a 7-role
crew) is concluded and preserved under /legacy in the repo.

## Architecture

- apps/sentinel — pluggable source adapters + LLM scorer; emits `SentimentSignal`
  envelopes onto the `hlpv2.sentiment` Redis stream.
- apps/oracle — orchestrator + HTTP API + WS broadcast. Runs three roles:
  - SNT: aggregate sentiment → `ProbabilityEstimate` (pHat, edge, confidence)
  - EXE: build `OutcomeProposal` (side, limit, Kelly-sized stake)
  - RSK: 13 fail-closed gates → `RiskDecision`
- packages/outcome-engine — pure math: aggregateSentiment, estimateProbability,
  computeEdge, kellyFraction, proposeOrder.
- packages/outcome-risk — pure gates: evaluate(RiskContext) → RiskDecision.
- packages/contracts — Zod schemas for OutcomeMarket, SentimentSignal,
  ProbabilityEstimate, OutcomeProposal, RiskDecision, OutcomeFill, RiskConfig.
- packages/event-bus — Redis Streams abstraction with in-memory fallback.
- packages/hl-client — Hyperliquid HTTP transport (kept from v1).

## Agent endpoints (v2)

Free:
- GET /healthz
- GET /v1/public/markets       — list of markets with pHat + edge (no sizes)
- GET /v1/public/floor         — mode + markets + recent role tape
- GET /v1/public/floor-tape    — recent role tape

(Agent-paid x402 endpoints from v1 are archived. v2 may add a smaller agent
surface — `/v1/agent/edges`, `/v1/agent/markets` — but is not yet wired in.)

## Risk gates (in evaluation order)

1. OPERATOR_HALT
2. INVALID_PROPOSAL
3. STALE_SENTIMENT
4. MARKET_NOT_TRADING
5. RESOLUTION_TOO_SOON
6. RESOLUTION_TOO_FAR
7. CHALLENGE_WINDOW_OPEN
8. EDGE_TOO_THIN
9. STAKE_PER_MARKET
10. CONCURRENT_MARKETS
11. CORRELATED_EXPOSURE
12. BANKROLL_DEPLETED
13. LOW_LIQUIDITY

Any failure → DENY (fail-closed, single-failure short-circuit).

## Key invariants (ported from v1)

- AI proposes, never executes. The risk engine is the only ALLOW path.
- Pure-function gates. Zero I/O. Deterministic.
- Hash-chained audit (SHA-256 prev-hash chain on hlpv2.audit).
- Privacy by default — public surface omits positions and notional.

## Repository documentation

- README.md         — repo overview and setup commands.
- packages/contracts/README.md     — Zod contracts for v2 messages.
- packages/outcome-engine/README.md — pure math API.
- packages/outcome-risk/README.md   — gate ordering and semantics.
- apps/sentinel/README.md           — sentiment ingestion service.
- apps/oracle/README.md             — orchestrator + HTTP/WS surface.
- legacy/README.md                  — v1 archive index.
